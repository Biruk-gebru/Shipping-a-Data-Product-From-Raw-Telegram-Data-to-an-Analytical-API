version: 2

sources:
  - name: medical_warehouse
    database: "{{ env_var('POSTGRES_DB') }}"
    schema: raw
    tables:
      - name: telegram_messages
        description: "Raw telegram messages"
      - name: yolo_detections
        description: "Raw object detection results from YOLO"
